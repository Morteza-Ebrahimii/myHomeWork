/*
Write two functions: one to calculate the area of a circle and another to calculate the circumference. (area = r*r*pi, circumference = 2*r*pi)
- area(3) = 28.27		
- circumference(3) = 18.85
*/
function area(radius1, radius2, pi) {
    console.log(radius1*radius2 && (radius1*radius2)*pi);
}
area(3, 3, 3.1415);



function circumference(radius, pi) {
    console.log(radius * 2 && (radius *2)* pi);
}
circumference(3, 3.1415);